import styled from "styled-components";
import sizes from "../../../infra/sizes";

export const IconContainerStyle = styled.div`
  display: flex;
  align-items: center;
  margin: ${sizes.header} 0 calc(${sizes.header} * 2) 0;
`;
