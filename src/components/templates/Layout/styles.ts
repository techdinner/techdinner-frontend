import styled from 'styled-components';

export const LayoutStyle = styled.div`
    display: flex;
    height: 100vh;

    .center{
        flex-grow: 1;
        display: flex;
        flex-direction: column;
    }
`;
